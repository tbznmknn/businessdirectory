'use server';

// This action is no longer needed since we're using an API route
// But keeping it for potential future use
export async function releaseKrakenAction() {
  // This will be handled by the API route instead
  return { success: false, error: 'Use API route instead' };
}
